import { Scene } from "./scene";

export default class CasioScene extends Scene {
    cameraLerpPosition = [0, 0, 0];

    constructor(...args) {
        super(...args);
    }

    sceneUpdate() {
        
    }
}